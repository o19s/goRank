version: "3"

services:
  app:
    image: golang:1.6.1-alpine
    volumes:
      - .:/go/src/goRank
    working_dir: /go/src/goRank
    command: go run app/goRank.go
    ports:
      - "8000:8000"
    depends_on:
      - cassandra
  cassandra:
    image: cassandra:3
    ports:
      - "9042"
